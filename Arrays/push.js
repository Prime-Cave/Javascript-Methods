// the push method of an array adds a new element to the end of an array and returns the new length of the

const randomNumbers = [2,3,5,1,5,1,6,2,8,2,0]
//push(element1, element2)
// Takes as many elements 
const updatedArray = randomNumbers.push(2)
// The Push Method would return the length of the array after it is updated.
console.log(updatedArray)